
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: Pure unit tests (no HTTP)
    integration: Tests that call the API endpoints
    slow: Tests that may take longer (e.g. scan submissions)
